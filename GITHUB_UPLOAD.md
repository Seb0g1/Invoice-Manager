# 📤 Инструкция по загрузке проекта в GitHub

## Предварительные требования

1. **Установите Git** (если еще не установлен):
   - Скачайте с https://git-scm.com/download/win
   - Или установите через `winget install Git.Git`

2. **Создайте репозиторий на GitHub**:
   - Перейдите на https://github.com/new
   - Создайте новый репозиторий с именем `Invoice-Manager`
   - **НЕ** инициализируйте его с README, .gitignore или лицензией

## Шаги для загрузки проекта

### 1. Откройте терминал в корне проекта

```bash
cd "C:\Users\Хуйню придумал\Downloads\david-warehouse"
```

### 2. Инициализируйте Git репозиторий

```bash
git init
```

### 3. Добавьте все файлы

```bash
git add .
```

### 4. Сделайте первый коммит

```bash
git commit -m "first commit"
```

### 5. Переименуйте ветку в main

```bash
git branch -M main
```

### 6. Добавьте удалённый репозиторий

```bash
git remote add origin https://github.com/Seb0g1/Invoice-Manager.git
```

### 7. Загрузите проект в GitHub

```bash
git push -u origin main
```

## Альтернативный способ (через GitHub Desktop)

1. Скачайте и установите GitHub Desktop: https://desktop.github.com/
2. Откройте GitHub Desktop
3. File → Add Local Repository
4. Выберите папку проекта
5. Нажмите "Publish repository"
6. Выберите репозиторий `Seb0g1/Invoice-Manager`

## Важно!

Перед загрузкой убедитесь, что:
- ✅ Файл `.env` добавлен в `.gitignore` (не будет загружен)
- ✅ `node_modules/` добавлены в `.gitignore`
- ✅ `dist/` и `build/` добавлены в `.gitignore`
- ✅ Загруженные файлы из `uploads/` не коммитятся (только структура)

## Если репозиторий уже существует

Если репозиторий уже создан и содержит файлы:

```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

## Проверка перед коммитом

Проверьте, что не коммитятся чувствительные данные:

```bash
git status
```

Убедитесь, что `.env` файлы не в списке!


