# 🚀 Настройка постоянного запуска Backend через PM2

## Проблема

Backend отключается при закрытии терминала. Нужно настроить автозапуск через PM2.

## Решение

### Вариант 1: Автоматическая настройка (рекомендуется)

```bash
cd /var/www/david-warehouse
chmod +x setup-pm2.sh
./setup-pm2.sh
```

Скрипт автоматически:
- Проверит и установит PM2 (если нужно)
- Проверит настройки .env
- Соберёт backend (если нужно)
- Запустит backend через PM2
- Настроит автозапуск при перезагрузке сервера

### Вариант 2: Ручная настройка

#### 1. Установка PM2

```bash
sudo npm install -g pm2
```

#### 2. Переход в директорию backend

```bash
cd /var/www/david-warehouse/backend
```

#### 3. Проверка .env

Убедитесь, что файл `.env` содержит:

```env
MONGO_URI=mongodb://gen_user:_*W%264xFfUJP9%2BO@147.45.175.217:27017/default_db?authSource=admin&directConnection=true
JWT_SECRET=your-secret-key-change-in-production-min-32-chars
PORT=5000
FRONTEND_URL=http://david.sakoo.ru
NODE_ENV=production
```

**Важно:** `FRONTEND_URL` должен совпадать с вашим доменом!

#### 4. Сборка backend (если еще не собрано)

```bash
npm run build
```

#### 5. Запуск через PM2

```bash
pm2 start dist/index.js --name invoice-backend
```

#### 6. Настройка автозапуска

```bash
# Настройка автозапуска (выполните команду, которую покажет PM2)
pm2 startup

# Сохранение текущей конфигурации
pm2 save
```

**Важно:** Команда `pm2 startup` покажет команду с `sudo`, которую нужно выполнить. Например:
```bash
sudo env PATH=$PATH:/usr/bin /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u your-username --hp /home/your-username
```

## Проверка работы

```bash
# Статус процессов
pm2 status

# Просмотр логов
pm2 logs invoice-backend

# Мониторинг в реальном времени
pm2 monit
```

## Полезные команды PM2

```bash
# Перезапуск
pm2 restart invoice-backend

# Остановка
pm2 stop invoice-backend

# Удаление из PM2
pm2 delete invoice-backend

# Просмотр логов
pm2 logs invoice-backend

# Просмотр последних 100 строк логов
pm2 logs invoice-backend --lines 100

# Очистка логов
pm2 flush

# Перезагрузка всех процессов
pm2 reload all

# Сохранение конфигурации
pm2 save
```

## Устранение проблем

### Проблема: 401 Unauthorized

**Причины:**
1. Backend не запущен
2. Неправильный FRONTEND_URL в .env
3. Проблемы с CORS

**Решение:**

1. Проверьте, что backend запущен:
   ```bash
   pm2 status
   ```

2. Проверьте логи:
   ```bash
   pm2 logs invoice-backend
   ```

3. Проверьте .env:
   ```bash
   cd /var/www/david-warehouse/backend
   cat .env | grep FRONTEND_URL
   ```
   
   Должно быть: `FRONTEND_URL=http://david.sakoo.ru`

4. Перезапустите backend:
   ```bash
   pm2 restart invoice-backend
   ```

### Проблема: Backend не запускается при перезагрузке

**Решение:**

```bash
# Проверьте автозапуск
pm2 startup

# Если не настроен, выполните команду, которую покажет PM2
# Затем сохраните конфигурацию
pm2 save
```

### Проблема: Порт 5000 занят

**Решение:**

```bash
# Проверьте, что занимает порт
sudo lsof -i :5000

# Или
sudo netstat -tulpn | grep 5000

# Остановите процесс или измените PORT в .env
```

### Проблема: Ошибки в логах

```bash
# Просмотр ошибок
pm2 logs invoice-backend --err

# Просмотр всех логов
pm2 logs invoice-backend
```

## Настройка переменных окружения в PM2

Если нужно изменить переменные окружения:

```bash
# Остановите процесс
pm2 stop invoice-backend
pm2 delete invoice-backend

# Запустите с переменными окружения
cd /var/www/david-warehouse/backend
pm2 start dist/index.js --name invoice-backend --env production

# Или создайте ecosystem файл
pm2 ecosystem
```

## Мониторинг

```bash
# Мониторинг в реальном времени
pm2 monit

# Информация о процессе
pm2 describe invoice-backend

# Статистика
pm2 list
```

## Автоматический перезапуск при сбоях

PM2 автоматически перезапускает процесс при сбоях. Это уже настроено по умолчанию.

Для настройки количества перезапусков:

```bash
pm2 start dist/index.js --name invoice-backend --max-restarts 10 --min-uptime 1000
```

## Обновление после изменений

После обновления кода:

```bash
cd /var/www/david-warehouse
./deploy.sh
```

Или вручную:

```bash
cd /var/www/david-warehouse/backend
npm run build
pm2 restart invoice-backend
```

## Проверка после настройки

1. **Проверьте статус:**
   ```bash
   pm2 status
   ```

2. **Проверьте логи:**
   ```bash
   pm2 logs invoice-backend --lines 50
   ```

3. **Проверьте в браузере:**
   - Откройте http://david.sakoo.ru
   - Должна открыться страница входа
   - Не должно быть ошибок 401 в консоли

4. **Перезагрузите сервер (опционально):**
   ```bash
   sudo reboot
   ```
   
   После перезагрузки проверьте:
   ```bash
   pm2 status
   ```
   
   Backend должен автоматически запуститься.

