# 📦 Установка зависимостей для исправления ошибок TypeScript

## Проблема

При сборке frontend возникают ошибки из-за отсутствующих зависимостей:
- `@tanstack/react-query`
- `react-hook-form`
- `@hookform/resolvers/yup`
- `tesseract.js`
- `yup`
- `@types/node`

## Решение

### На сервере выполните:

```bash
cd /var/www/davidsklad/frontend
npm install
npm run build
```

### Или установите зависимости вручную:

```bash
cd /var/www/davidsklad/frontend
npm install @tanstack/react-query@^5.17.0 @tanstack/react-query-devtools@^5.17.0 react-hook-form@^7.49.0 @hookform/resolvers@^3.3.2 yup@^1.3.3 tesseract.js@^5.0.4 @types/node@^20.10.0
npm run build
```

## Что было исправлено

1. ✅ Добавлены все недостающие зависимости в `package.json`
2. ✅ Удалены неиспользуемые переменные и импорты
3. ✅ Добавлены типы для параметров функций
4. ✅ Исправлены типы для NodeJS (`ReturnType<typeof setTimeout>`)
5. ✅ Исправлен `process.env.NODE_ENV` на `import.meta.env.DEV`

## После установки

После установки зависимостей сборка должна пройти успешно:

```bash
npm run build
```

---

**Файл обновлен:** `frontend/package.json`

