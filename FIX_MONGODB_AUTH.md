# 🔐 Исправление ошибки аутентификации MongoDB

## Проблема

```
MongoServerError: Authentication failed.
```

Ошибка означает, что пользователь не существует или пароль неправильный.

## 🔍 Диагностика

### Шаг 1: Проверка, включена ли аутентификация

```bash
# Проверка конфигурации MongoDB
sudo cat /etc/mongod.conf | grep -A 2 "security:"
```

Если аутентификация включена, вы увидите:
```yaml
security:
  authorization: enabled
```

### Шаг 2: Подключение без аутентификации (если она не включена)

```bash
# Попробуйте подключиться без пароля
mongosh
```

Если подключение успешно, значит аутентификация не включена.

## 🔧 Решения

### Решение 1: Отключить аутентификацию (для локального использования)

Если MongoDB используется только локально, можно отключить аутентификацию:

```bash
# 1. Редактирование конфигурации
sudo nano /etc/mongod.conf
```

Найдите секцию `security:` и закомментируйте или удалите:
```yaml
# security:
#   authorization: enabled
```

Или просто удалите эти строки.

```bash
# 2. Перезапуск MongoDB
sudo systemctl restart mongod

# 3. Обновление .env файла
cd /var/www/davidsklad/backend
nano .env
```

Измените `MONGO_URI` на:
```env
MONGO_URI=mongodb://127.0.0.1:27017/invoice-db
```

```bash
# 4. Перезапуск приложения
pm2 restart davidsklad-backend
```

### Решение 2: Создать пользователя правильно

Если нужно использовать аутентификацию:

```bash
# 1. Подключение без аутентификации (если она еще не включена)
mongosh

# 2. Переключение на базу данных
use invoice-db

# 3. Создание пользователя
db.createUser({
  user: "invoice_user",
  pwd: "CGJ-Ge-90",
  roles: [ { role: "readWrite", db: "invoice-db" } ]
})

# 4. Проверка создания пользователя
db.getUsers()

# 5. Выход
exit
```

```bash
# 6. Включение аутентификации в конфигурации
sudo nano /etc/mongod.conf
```

Добавьте или раскомментируйте:
```yaml
security:
  authorization: enabled
```

```bash
# 7. Перезапуск MongoDB
sudo systemctl restart mongod

# 8. Проверка подключения с аутентификацией
mongosh -u invoice_user -p CGJ-Ge-90 --authenticationDatabase invoice-db
```

Если подключение успешно, обновите `.env`:
```env
MONGO_URI=mongodb://invoice_user:CGJ-Ge-90@127.0.0.1:27017/invoice-db?authSource=invoice-db
```

### Решение 3: Использовать admin базу для аутентификации

Иногда пользователь должен быть создан в базе `admin`:

```bash
# 1. Подключение без аутентификации
mongosh

# 2. Создание пользователя в admin базе
use admin
db.createUser({
  user: "invoice_user",
  pwd: "CGJ-Ge-90",
  roles: [
    { role: "readWrite", db: "invoice-db" },
    { role: "readWrite", db: "admin" }
  ]
})

# 3. Выход
exit
```

Затем используйте в `.env`:
```env
MONGO_URI=mongodb://invoice_user:CGJ-Ge-90@127.0.0.1:27017/invoice-db?authSource=admin
```

## ✅ Быстрое решение (рекомендуется для локального использования)

```bash
# 1. Отключить аутентификацию
sudo nano /etc/mongod.conf
# Закомментируйте или удалите секцию security:

# 2. Перезапустить MongoDB
sudo systemctl restart mongod

# 3. Обновить .env
cd /var/www/davidsklad/backend
sed -i 's|mongodb://.*@127.0.0.1:27017|mongodb://127.0.0.1:27017|g' .env
# Или вручную:
nano .env
# Установите: MONGO_URI=mongodb://127.0.0.1:27017/invoice-db

# 4. Перезапустить приложение
pm2 restart davidsklad-backend
```

## 🔍 Проверка текущих пользователей

```bash
# Подключение без аутентификации
mongosh

# Просмотр пользователей в базе invoice-db
use invoice-db
db.getUsers()

# Просмотр пользователей в базе admin
use admin
db.getUsers()

# Выход
exit
```

## 🗑️ Удаление пользователя (если нужно пересоздать)

```bash
mongosh

# Удаление пользователя из invoice-db
use invoice-db
db.dropUser("invoice_user")

# Удаление пользователя из admin
use admin
db.dropUser("invoice_user")

exit
```

## 📋 Полная последовательность для локального использования (без аутентификации)

```bash
# 1. Проверка конфигурации
sudo cat /etc/mongod.conf | grep -A 2 "security:"

# 2. Отключение аутентификации (если включена)
sudo nano /etc/mongod.conf
# Удалите или закомментируйте:
# security:
#   authorization: enabled

# 3. Перезапуск MongoDB
sudo systemctl restart mongod

# 4. Проверка подключения
mongosh

# 5. Создание базы данных (если нужно)
use invoice-db
db.test.insertOne({test: true})
db.test.deleteOne({test: true})

# 6. Выход
exit

# 7. Обновление .env
cd /var/www/davidsklad/backend
nano .env
# MONGO_URI=mongodb://127.0.0.1:27017/invoice-db

# 8. Перезапуск приложения
pm2 restart davidsklad-backend

# 9. Проверка логов
pm2 logs davidsklad-backend --lines 20
```

---

**Рекомендация:** Для локального использования на одном сервере лучше отключить аутентификацию MongoDB. Это упростит управление и не повлияет на безопасность, так как MongoDB будет доступен только локально.

