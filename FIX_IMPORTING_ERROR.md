# 🔧 Исправление ошибки "importing is not defined"

## Проблема

При использовании функции импорта Excel на странице "Наш склад" возникает ошибка:
```
importing is not defined
```

## Причина

В компоненте `Warehouse.tsx` использовалась переменная состояния `importing` и функция `setImporting`, но переменная не была объявлена через `useState`.

Также отсутствовал импорт `useQueryClient` для работы с React Query.

## Решение

### 1. Добавлено состояние `importing`

```typescript
const [importing, setImporting] = useState(false);
```

### 2. Добавлен импорт `useQueryClient`

```typescript
import { useQueryClient } from '@tanstack/react-query';
```

### 3. Инициализирован `queryClient`

```typescript
const queryClient = useQueryClient();
```

## Исправленные файлы

- ✅ `frontend/src/pages/Warehouse.tsx` - добавлено состояние `importing` и импорт `useQueryClient`

## Проверка

После обновления кода:

1. Перезагрузите страницу в браузере
2. Откройте диалог импорта Excel на странице "Наш склад"
3. Ошибка должна исчезнуть
4. Импорт должен работать корректно

## Если используете Git

Обновите проект на сервере:

```bash
cd /var/www/davidsklad
git pull origin main
cd frontend
npm install
npm run build
```

---

**Файл обновлен:** `frontend/src/pages/Warehouse.tsx`

