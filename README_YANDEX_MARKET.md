# Система интеграции с Яндекс Маркет Partner API

## ✅ Реализованный функционал

### 1. Модели базы данных
- ✅ `YandexBusiness` - управление бизнесами
- ✅ `YandexProduct` - товары, объединенные по артикулу
- ✅ `YandexProductBusinessLink` - связи товаров с бизнесами

### 2. Сервис для работы с API
- ✅ Загрузка всех офферов с автоматической пагинацией
- ✅ Загрузка остатков по складам
- ✅ Загрузка цен с пагинацией
- ✅ Массовое обновление цен
- ✅ Обработка rate limits и retry логика
- ✅ Кэширование токенов

### 3. Контроллеры
- ✅ Полная синхронизация товаров
- ✅ Синхронизация остатков
- ✅ Синхронизация цен
- ✅ Получение товара по артикулу со всеми бизнесами
- ✅ Обновление цены во всех бизнесах
- ✅ Массовое обновление цен
- ✅ Поиск товаров
- ✅ Получение списка всех товаров с пагинацией

### 4. Управление бизнесами
- ✅ CRUD операции для бизнесов
- ✅ Тестирование подключения к API
- ✅ UI для управления бизнесами

### 5. Планировщик задач
- ✅ Автоматическая полная синхронизация (раз в сутки в 3:00)
- ✅ Автоматическая синхронизация остатков (каждый час)
- ✅ Автоматическая синхронизация цен (каждый час в :30)

### 6. Фронтенд страницы
- ✅ Управление бизнесами (`/yandex-market/businesses`)
- ✅ Управление ценами (`/price-control`)
- ✅ Список всех товаров (`/yandex-market/products`)
- ✅ Статистика (`/yandex-market/stats`)

### 7. Статистика
- ✅ Общая статистика по товарам
- ✅ Статистика по ценам
- ✅ Статистика по остаткам
- ✅ Статистика по каждому бизнесу

## 📋 Структура проекта

### Backend
```
backend/src/
├── models/
│   ├── YandexBusiness.ts
│   ├── YandexProduct.ts
│   └── YandexProductBusinessLink.ts
├── services/
│   ├── yandexMarketService.ts
│   └── schedulerService.ts
├── controllers/
│   ├── yandexMarketController.ts
│   ├── yandexMarketBulkController.ts
│   ├── yandexBusinessController.ts
│   └── yandexMarketStatsController.ts
└── routes/
    └── index.ts (добавлены роуты для Яндекс Маркет)
```

### Frontend
```
frontend/src/
├── pages/
│   ├── YandexBusinesses.tsx
│   ├── YandexPriceControl.tsx
│   ├── YandexMarketProducts.tsx
│   └── YandexMarketStats.tsx
└── components/
    ├── AppRoutes.tsx (добавлены роуты)
    └── Sidebar.tsx (добавлены пункты меню)
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd backend
npm install node-cron @types/node-cron
```

### 2. Настройка бизнесов

1. Откройте страницу **Yandex Market → Бизнесы**
2. Добавьте бизнесы с их токенами доступа
3. Протестируйте подключение для каждого бизнеса

### 3. Первая синхронизация

Запустите полную синхронизацию через API:
```bash
POST /api/yandex-market/sync/products
```

Или используйте интерфейс настроек.

### 4. Использование

- **Управление ценами**: `/price-control` - обновление цены одного товара во всех бизнесах
- **Просмотр товаров**: `/yandex-market/products` - список всех товаров
- **Статистика**: `/yandex-market/stats` - общая статистика

## 📊 Особенности

1. **Объединение по артикулу**: Товары с одинаковым `vendorCode` автоматически объединяются
2. **Автоматическая синхронизация**: Работает в фоновом режиме
3. **Массовые операции**: Поддержка обновления цен для множества товаров
4. **Обработка ошибок**: Автоматический retry при rate limits
5. **Масштабируемость**: Оптимизировано для работы с 10k-50k товаров

## 📝 API Документация

См. файл `YANDEX_MARKET_INTEGRATION.md` для полной документации API.

## 🔧 Настройка

См. файл `YANDEX_MARKET_SETUP.md` для подробных инструкций по настройке.

## ⚠️ Важные замечания

1. Токены доступа должны быть валидными OAuth токенами от Яндекс Маркета
2. Система автоматически обрабатывает пагинацию для всех методов API
3. Rate limits обрабатываются автоматически с задержками и retry
4. Автоматическая синхронизация запускается при старте сервера

## 🐛 Устранение неполадок

Если возникают проблемы:
1. Проверьте токены доступа через кнопку тестирования
2. Проверьте логи сервера
3. Убедитесь, что бизнесы включены (`enabled: true`)
4. Проверьте, что товары синхронизированы

