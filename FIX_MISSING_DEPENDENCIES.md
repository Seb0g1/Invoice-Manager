# 🔧 Исправление ошибок отсутствующих зависимостей

## Проблема

При сборке проекта возникают ошибки:
```
error TS2307: Cannot find module 'googleapis'
error TS2307: Cannot find module 'google-auth-library'
error TS2307: Cannot find module 'sharp'
```

## Причина

В `package.json` отсутствуют необходимые зависимости:
- `googleapis` - для работы с Google Sheets API
- `google-auth-library` - для OAuth2 аутентификации Google
- `sharp` - для оптимизации изображений

## Решение

### Вариант 1: Установить зависимости вручную

На сервере выполните:

```bash
cd /var/www/davidsklad/backend
npm install googleapis google-auth-library sharp
npm run build
```

### Вариант 2: Обновить package.json и установить все зависимости

Файл `package.json` уже обновлен. Выполните:

```bash
cd /var/www/davidsklad/backend
npm install
npm run build
```

### Вариант 3: Если используете Git

Если проект уже на GitHub, обновите его на сервере:

```bash
cd /var/www/davidsklad
git pull origin main
cd backend
npm install
npm run build
```

## Обновленные зависимости

В `package.json` добавлены:
- `googleapis: ^126.0.0` - Google APIs клиент
- `google-auth-library: ^9.0.0` - OAuth2 библиотека
- `sharp: ^0.32.6` - Оптимизация изображений

## Проверка установки

После установки проверьте:

```bash
cd /var/www/davidsklad/backend
npm list googleapis google-auth-library sharp
```

Должны быть видны установленные версии пакетов.

## Примечание

Эти зависимости используются для:
- **googleapis/google-auth-library**: Интеграция с Google Sheets (опционально, можно отключить если не используется)
- **sharp**: Оптимизация загружаемых изображений накладных (рекомендуется)

Если Google Sheets не используется, можно удалить соответствующие импорты и зависимости.

---

**Файл обновлен:** `backend/package.json`

