# Инструкция по настройке Market Yandex Go API

## Описание

Система интеграции с Market Yandex Go API для управления товарами в нескольких бизнесах. Товары объединяются по артикулу (vendorCode), что позволяет управлять одним товаром во всех бизнесах из одного места.

## Отличия от обычного Yandex Market API

1. **Базовый URL**: `https://api.partner.market.yandex.net/` (вместо `.ru`)
2. **Авторизация**: `Api-Key` заголовок (вместо OAuth)
3. **Методы**: Используются методы v2 API

## Быстрый старт

### 1. Добавление бизнесов

1. Перейдите в раздел **Yandex Market → Бизнесы**
2. Нажмите "Добавить бизнес"
3. Заполните форму:
   - **Business ID** - ID бизнеса из Market Yandex Go
   - **Название** - Название для удобства
   - **Api-Key** - Api-Key для доступа к API (обязательно)
   - **Включен для синхронизации** - Включить/выключить бизнес

4. Нажмите "Сохранить"
5. Нажмите кнопку тестирования (🔄) для проверки подключения

### 2. Первая синхронизация

После добавления бизнесов выполните полную синхронизацию:

**Через API:**
```bash
POST /api/yandex-market-go/sync/products
```

**Или через интерфейс:**
- Перейдите в раздел настроек
- Найдите раздел "Яндекс Маркет Go"
- Нажмите "Синхронизировать товары"

### 3. Настройка автоматической синхронизации

Автоматическая синхронизация уже настроена и работает автоматически:

- **Полная синхронизация товаров**: Каждый день в 3:00
- **Синхронизация остатков**: Каждый час (в :00)
- **Синхронизация цен**: Каждый час (в :30)

## Использование

### Управление ценами

1. Перейдите в **Yandex Market → Обновление цен (Go)**
2. Введите артикул товара
3. Просмотрите список всех бизнесов, где есть этот товар
4. Введите новую цену
5. Нажмите "Применить для всех бизнесов"

### Обновление названий и описаний

1. На странице управления ценами нажмите "Изменить название"
2. Введите новое название и/или описание
3. Нажмите "Обновить"

Система автоматически обновит название/описание во всех бизнесах.

### Просмотр всех товаров

1. Перейдите в **Yandex Market → Все товары (Market)**
2. Используйте поиск для фильтрации
3. Просматривайте товары со всеми бизнесами

## API Endpoints

### Управление бизнесами
- `GET /api/yandex-market/businesses` - Список бизнесов
- `GET /api/yandex-market/businesses/:id` - Получить бизнес
- `POST /api/yandex-market/businesses` - Создать бизнес
- `PUT /api/yandex-market/businesses/:id` - Обновить бизнес
- `DELETE /api/yandex-market/businesses/:id` - Удалить бизнес
- `POST /api/yandex-market/businesses/:id/test` - Тест подключения

### Синхронизация (Market Yandex Go)
- `POST /api/yandex-market-go/sync/products` - Полная синхронизация товаров
- `POST /api/yandex-market-go/sync/stocks` - Синхронизация остатков
- `POST /api/yandex-market-go/sync/prices` - Синхронизация цен

### Работа с товарами (Market Yandex Go)
- `GET /api/yandex-market-go/products` - Список товаров (с пагинацией)
- `GET /api/yandex-market-go/products/search` - Поиск товаров
- `GET /api/yandex-market-go/products/:vendorCode` - Получить товар по артикулу
- `POST /api/yandex-market-go/products/update-price` - Обновить цену одного товара
- `POST /api/yandex-market-go/products/update-name` - Обновить название и описание

## Получение Api-Key

Для получения Api-Key:

1. Зайдите в личный кабинет Market Yandex Go
2. Перейдите в раздел "Настройки API"
3. Создайте API ключ
4. Скопируйте ключ
5. Добавьте его в систему через интерфейс управления бизнесами

## Особенности реализации

### Обработка больших объемов (10k-30k товаров)

1. **Асинхронные очереди**: Используется `queueService` для параллельной обработки
2. **Батчи**: Обработка по 50-200 товаров за раз
3. **Retry логика**: Автоматические повторы при ошибках
4. **Rate limit handling**: Автоматическая обработка ограничений API

### Объединение товаров

Товары с одинаковым `vendorCode` автоматически объединяются в один товар в системе. Это позволяет:
- Управлять одним товаром во всех бизнесах
- Обновлять цену один раз для всех бизнесов
- Видеть все бизнесы, где есть товар

### Массовые операции

- Обновление цен: батчи по 100 товаров
- Обновление остатков: батчи по 100 offerIds
- Обновление названий: по одному офферу за раз (требование API)

## Важные замечания

1. **Api-Key обязателен**: Без Api-Key синхронизация не будет работать
2. **Один Api-Key на бизнес**: Каждый бизнес должен иметь свой Api-Key
3. **Автоматическая синхронизация**: Запускается при старте сервера
4. **Rate Limits**: Система автоматически обрабатывает ограничения API

## Устранение неполадок

### Товары не синхронизируются
- Проверьте, что бизнес включен (`enabled: true`)
- Проверьте Api-Key
- Используйте кнопку тестирования подключения

### Цены не обновляются
- Проверьте, что товар найден в системе
- Проверьте, что товар присутствует в бизнесе
- Проверьте логи сервера на наличие ошибок

### Остатки показывают 0
- Запустите синхронизацию остатков вручную
- Проверьте, что товары есть в бизнесе
- Проверьте статус товара в Market Yandex Go

## Структура базы данных

### YandexBusiness
- `businessId` - ID бизнеса
- `name` - Название
- `apiKey` - Api-Key для доступа
- `enabled` - Включен ли бизнес

### YandexProduct
- `vendorCode` - Артикул (ключевой идентификатор)
- `name` - Название
- `description` - Описание
- `images` - Изображения
- `category` - Категория
- `mainProductId` - ID основного товара (если объединен)

### YandexProductBusinessLink
- `productId` - Ссылка на товар
- `businessId` - ID бизнеса
- `offerId` - ID оффера
- `sku` - SKU
- `price` - Цена
- `stock` - Остатки
- `lastSync` - Время последней синхронизации

