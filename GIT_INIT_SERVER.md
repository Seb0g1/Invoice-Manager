# 🔧 Инициализация Git репозитория на сервере

Если вы получили ошибку "Это не Git репозиторий" при запуске `deploy.sh`, значит проект на сервере не был клонирован через Git.

## Решение

### Вариант 1: Автоматическая инициализация (рекомендуется)

```bash
cd /var/www/david-warehouse
chmod +x init-git-repo.sh
./init-git-repo.sh
```

Скрипт автоматически:
- Проверит наличие Git
- Инициализирует репозиторий
- Настроит связь с GitHub
- Предложит варианты действий

### Вариант 2: Переустановка проекта (если нет важных локальных изменений)

Если на сервере нет важных изменений, проще всего переустановить проект:

```bash
# Создайте резервную копию .env и других важных файлов
cd /var/www
cp -r david-warehouse/backend/.env /tmp/backend.env.backup 2>/dev/null || true

# Удалите старую директорию
sudo rm -rf david-warehouse

# Клонируйте проект заново
git clone https://github.com/Seb0g1/Invoice-Manager.git david-warehouse

# Восстановите .env
cp /tmp/backend.env.backup david-warehouse/backend/.env 2>/dev/null || true

# Настройте проект
cd david-warehouse
chmod +x setup.sh
./setup.sh
```

### Вариант 3: Ручная инициализация

```bash
cd /var/www/david-warehouse

# Инициализация Git
git init
git remote add origin https://github.com/Seb0g1/Invoice-Manager.git
git branch -M main

# Получение данных с GitHub
git fetch origin

# Если есть локальные изменения, добавьте их
git add .
git commit -m "Initial commit from server"

# Свяжите с удаленной веткой
git branch --set-upstream-to=origin/main main

# Получите изменения (может потребоваться разрешить конфликты)
git pull origin main --allow-unrelated-histories
```

## После инициализации

1. **Убедитесь, что файл .env настроен:**
   ```bash
   cd /var/www/david-warehouse/backend
   nano .env
   ```

2. **Установите зависимости и соберите проект:**
   ```bash
   cd /var/www/david-warehouse
   chmod +x setup.sh
   ./setup.sh
   ```

3. **Теперь можно использовать deploy.sh:**
   ```bash
   cd /var/www/david-warehouse
   chmod +x deploy.sh
   ./deploy.sh
   ```

## Проверка

Проверьте, что всё настроено правильно:

```bash
cd /var/www/david-warehouse
git status
git remote -v
git branch -a
```

Должно показать:
- Связь с `origin` (GitHub)
- Текущую ветку `main`
- Статус репозитория

## Важно!

⚠️ **Перед удалением директории убедитесь, что:**
- Сохранён файл `.env` из `backend/.env`
- Сохранены загруженные файлы из `backend/uploads/` (если нужно)
- Сохранены любые другие локальные изменения

## Если возникли проблемы

1. **Проверьте права доступа:**
   ```bash
   sudo chown -R $USER:$USER /var/www/david-warehouse
   ```

2. **Проверьте, что Git установлен:**
   ```bash
   git --version
   # Если нет: sudo apt install git -y
   ```

3. **Проверьте подключение к GitHub:**
   ```bash
   ping github.com
   ```

4. **Проверьте логи:**
   ```bash
   git log --oneline -5
   ```

