# 📦 Инструкция по установке проекта

## 🚀 Полная автоматическая установка (рекомендуется)

Для полной установки проекта на сервер используйте скрипт `full-install.sh`:

```bash
# 1. Загрузите скрипт на сервер (или клонируйте репозиторий)
cd /var/www
sudo git clone https://github.com/YOUR_USERNAME/david-warehouse.git davidsklad
cd davidsklad

# 2. Сделайте скрипт исполняемым
sudo chmod +x full-install.sh

# 3. Запустите установку
sudo ./full-install.sh https://github.com/YOUR_USERNAME/david-warehouse.git
```

Скрипт автоматически:
- ✅ Обновит систему
- ✅ Установит Git, Node.js, PM2, Nginx, Certbot
- ✅ Проверит MongoDB
- ✅ Клонирует проект с GitHub
- ✅ Создаст .env файлы с базовыми настройками
- ✅ Установит зависимости и соберет проект
- ✅ Настроит PM2
- ✅ Настроит Nginx
- ✅ Настроит Firewall

## 📋 Пошаговая установка (вручную)

Если хотите установить вручную, следуйте инструкциям:

1. **Настройка сервера:** `setup-server.sh`
2. **Клонирование и деплой:** `DEPLOY_FROM_GITHUB.md`

## 🔄 Обновление проекта

После установки для обновления используйте:

```bash
cd /var/www/davidsklad
./update-from-github.sh
```

Или:

```bash
cd /var/www/davidsklad
./deploy.sh --pull
```

## 📝 Что нужно сделать после установки

1. **Отредактируйте переменные окружения:**
   ```bash
   nano /var/www/davidsklad/backend/.env
   nano /var/www/davidsklad/frontend/.env.production
   ```

2. **Установите SSL сертификат:**
   ```bash
   sudo certbot --nginx -d davidsklad.ru -d www.davidsklad.ru
   ```

3. **Проверьте работу:**
   ```bash
   pm2 status
   pm2 logs davidsklad-backend
   ```

4. **Откройте в браузере:**
   ```
   https://davidsklad.ru
   ```

## 🛠️ Доступные скрипты

- **`full-install.sh`** - Полная автоматическая установка
- **`setup-server.sh`** - Настройка сервера (установка зависимостей)
- **`deploy.sh`** - Деплой проекта (сборка и запуск)
- **`update-from-github.sh`** - Обновление с GitHub
- **`BACKUP_MONGODB.sh`** - Резервное копирование MongoDB

---

**Подробные инструкции:**
- Полная установка: `full-install.sh`
- Деплой с GitHub: `DEPLOY_FROM_GITHUB.md`
- Быстрый деплой: `QUICK_DEPLOY_FROM_GITHUB.md`

