# 🔧 Исправление ошибки "tsc: not found"

## Проблема

При выполнении сборки проекта возникает ошибка:
```
sh: 1: tsc: not found
```

## Причина

Ошибка возникает потому, что TypeScript компилятор (`tsc`) находится в `devDependencies`, а при использовании `npm install --production` devDependencies не устанавливаются.

## Решение

### Вариант 1: Установить все зависимости (включая devDependencies)

Для сборки проекта нужны все зависимости, включая devDependencies:

```bash
cd /var/www/davidsklad/backend
npm install  # Без флага --production
npm run build

cd ../frontend
npm install  # Без флага --production
npm run build
```

### Вариант 2: Установить TypeScript глобально (не рекомендуется)

```bash
sudo npm install -g typescript
```

Но лучше использовать Вариант 1, так как версия TypeScript должна соответствовать версии в проекте.

## Исправленные скрипты

Все скрипты обновлены:
- ✅ `full-install.sh` - теперь использует `npm install` вместо `npm install --production`
- ✅ `deploy.sh` - обновлен
- ✅ `update-from-github.sh` - обновлен

## Быстрое исправление

Если вы уже запустили установку и получили ошибку:

```bash
cd /var/www/davidsklad/backend
npm install  # Установит все зависимости включая tsc
npm run build

cd ../frontend
npm install
npm run build

# Затем перезапустите приложение
cd ../backend
pm2 restart davidsklad-backend
```

## Почему не использовать --production?

Флаг `--production` используется для установки только production зависимостей, но для **сборки** проекта (компиляции TypeScript, минификации и т.д.) нужны devDependencies:
- `typescript` - для компиляции TS в JS
- `vite` - для сборки frontend
- `@types/*` - типы TypeScript
- И другие инструменты сборки

После сборки можно удалить devDependencies, но это не обязательно, так как они не влияют на работу приложения в production.

---

**Примечание:** Скрипты обновлены и теперь используют `npm install` для установки всех зависимостей перед сборкой.

