# 🔧 Устранение проблем с запуском Frontend

## Проблема: Frontend не запускается

### 1. Проверка установки зависимостей

```bash
cd frontend
npm install
```

Если возникают ошибки, попробуйте:

```bash
# Очистка кэша и переустановка
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

### 2. Проверка версии Node.js

Требуется Node.js v18 или выше:

```bash
node --version
```

Если версия ниже 18, обновите Node.js:
- Windows: https://nodejs.org/
- Linux: `nvm install 18 && nvm use 18`

### 3. Проверка порта

Frontend запускается на порту **3000** (см. `vite.config.ts`).

Если порт занят, измените порт в `vite.config.ts`:

```typescript
server: {
  port: 3001, // или другой свободный порт
}
```

Или убейте процесс, занимающий порт:

```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Linux/Mac
lsof -ti:3000 | xargs kill
```

### 4. Проверка TypeScript ошибок

Перед запуском убедитесь, что нет ошибок TypeScript:

```bash
cd frontend
npm run build
```

Если есть ошибки, исправьте их перед запуском dev сервера.

### 5. Использование скриптов запуска

**Windows:**
```bash
start-frontend.bat
```

**Linux/Mac:**
```bash
chmod +x start-frontend.sh
./start-frontend.sh
```

Скрипты автоматически:
- Проверят наличие Node.js
- Установят зависимости (если нужно)
- Запустят dev сервер

### 6. Ручной запуск

```bash
cd frontend
npm install  # если еще не установлено
npm run dev
```

Frontend должен быть доступен на: **http://localhost:3000**

### 7. Проверка конфигурации Vite

Убедитесь, что файл `vite.config.ts` существует и содержит:

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:5000',
        changeOrigin: true
      }
    }
  }
});
```

### 8. Проверка файлов проекта

Убедитесь, что существуют:
- `frontend/index.html`
- `frontend/src/main.tsx`
- `frontend/src/App.tsx`
- `frontend/package.json`

### 9. Ошибки при сборке

Если `npm run build` выдает ошибки:

1. Проверьте TypeScript ошибки:
   ```bash
   cd frontend
   npx tsc --noEmit
   ```

2. Проверьте линтер:
   ```bash
   npm run lint  # если есть скрипт lint
   ```

3. Убедитесь, что все зависимости установлены:
   ```bash
   npm install
   ```

### 10. Проблемы с кэшем

Если возникают странные ошибки, очистите кэш:

```bash
cd frontend
rm -rf node_modules .vite dist
npm cache clean --force
npm install
npm run dev
```

## Частые ошибки

### "Cannot find module 'vite'"

```bash
cd frontend
npm install
```

### "Port 3000 is already in use"

Измените порт в `vite.config.ts` или освободите порт (см. пункт 3).

### "EADDRINUSE: address already in use"

Порт занят другим процессом. Освободите порт или измените его.

### "TypeError: Failed to fetch"

Backend не запущен или недоступен на порту 5000. Убедитесь, что backend запущен.

## Проверка работы

После запуска frontend должен:

1. ✅ Открыться на http://localhost:3000
2. ✅ Показать страницу входа
3. ✅ Подключаться к backend на http://localhost:5000/api

## Логи и отладка

Для более подробных логов:

```bash
cd frontend
npm run dev -- --debug
```

Или проверьте консоль браузера (F12) на наличие ошибок.

## Нужна помощь?

Если проблема не решена:

1. Проверьте логи в терминале
2. Проверьте консоль браузера (F12)
3. Убедитесь, что backend запущен
4. Проверьте версии Node.js и npm

