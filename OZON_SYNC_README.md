# Синхронизация товаров OZON

Система для быстрой синхронизации ~13,000 товаров из OZON Seller API с использованием курсорной пагинации и параллельной загрузки данных.

## Возможности

- ✅ Курсорная пагинация через `last_id` для эффективной обработки большого количества товаров
- ✅ Параллельная загрузка данных (названия, цены, остатки, изображения)
- ✅ Оптимизированная обработка батчами по 1000 товаров
- ✅ Автоматическая обработка rate limits с retry логикой
- ✅ Сохранение всех данных в MongoDB с использованием bulk operations

## Используемые методы API

1. **POST /v3/product/info/list** - получение списка товаров с курсорной пагинацией
2. **POST /v4/product/info/prices** - получение цен товаров
3. **POST /v4/product/info/stocks** - получение остатков товаров
4. **POST /v2/product/pictures/info** - получение изображений товаров

## Запуск синхронизации

### Через API (рекомендуется)

1. Откройте приложение и перейдите в раздел **Настройки** → **Ozon API**
2. Убедитесь, что API настроен (Client ID и API Key указаны)
3. Нажмите кнопку **Синхронизировать товары** или отправьте GET запрос:
   ```bash
   curl -X GET http://localhost:5000/api/ozon/sync \
     -H "Cookie: token=YOUR_TOKEN"
   ```

### Через командную строку

```bash
cd backend
npm run sync-ozon
```

Или напрямую через ts-node:

```bash
cd backend
npx ts-node scripts/sync-ozon-products.ts
```

### Через cron (автоматическая синхронизация)

Добавьте в crontab для ежедневной синхронизации в 3:00 ночи:

```bash
0 3 * * * cd /path/to/david-warehouse/backend && npm run sync-ozon >> /var/log/ozon-sync.log 2>&1
```

Или через systemd timer (рекомендуется для systemd-based систем):

Создайте файл `/etc/systemd/system/ozon-sync.service`:
```ini
[Unit]
Description=OZON Products Sync
After=network.target

[Service]
Type=oneshot
User=www-data
WorkingDirectory=/var/www/david-warehouse/backend
Environment="NODE_ENV=production"
ExecStart=/usr/bin/npm run sync-ozon
```

Создайте файл `/etc/systemd/system/ozon-sync.timer`:
```ini
[Unit]
Description=Run OZON Products Sync daily
Requires=ozon-sync.service

[Timer]
OnCalendar=daily
OnCalendar=03:00
Persistent=true

[Install]
WantedBy=timers.target
```

Активируйте timer:
```bash
sudo systemctl enable ozon-sync.timer
sudo systemctl start ozon-sync.timer
```

## Процесс синхронизации

Синхронизация проходит в несколько этапов:

1. **Получение списка товаров** - загрузка всех товаров через `/v3/product/info/list` с курсорной пагинацией
2. **Загрузка цен** - параллельная загрузка цен через `/v4/product/info/prices`
3. **Загрузка остатков** - параллельная загрузка остатков через `/v4/product/info/stocks`
4. **Загрузка изображений** - загрузка изображений батчами по 1000 товаров через `/v2/product/pictures/info`
5. **Сохранение в БД** - объединение всех данных и сохранение в MongoDB батчами по 1000 операций

## Данные, которые сохраняются

Для каждого товара сохраняются:

- `productId` - ID товара в OZON
- `offerId` - артикул продавца
- `sku` - SKU товара
- `name` - название товара
- `price` - текущая цена
- `oldPrice` - старая цена (если есть)
- `currency` - валюта (обычно RUB)
- `status` - статус товара
- `images` - массив URL изображений
- `primaryImage` - основное изображение
- `stock.present` - остаток на складе (сумма по всем складам)
- `stock.reserved` - зарезервировано
- `hasPrice` - есть ли цена
- `hasStock` - есть ли остатки
- `createdAtOzon` - дата создания в OZON
- `updatedAtOzon` - дата обновления в OZON
- `syncedAt` - дата последней синхронизации

## Производительность

При синхронизации ~13,000 товаров:

- **Время выполнения**: ~5-10 минут (зависит от скорости API и сети)
- **Скорость**: ~20-40 товаров/сек
- **Использование памяти**: ~200-500 MB
- **Нагрузка на API**: контролируется задержками между запросами (100ms)

## Обработка ошибок

- Автоматический retry при rate limit ошибках (429)
- Экспоненциальная задержка при повторных попытках
- Продолжение работы при ошибках получения отдельных данных (цен, остатков, изображений)
- Логирование всех ошибок в консоль

## Мониторинг

Прогресс синхронизации отображается в консоли:

```
Получение списка товаров... - 5000/13000 (38%)
Загрузка цен... - 13000/13000 (100%)
Загрузка остатков... - 13000/13000 (100%)
Загрузка изображений... - 13000/13000 (100%)
Сохранение в базу данных... - 13000/13000 (100%)
```

После завершения выводится статистика:

```
✅ Синхронизация завершена!
📊 Статистика:
   - Всего товаров: 13000
   - Синхронизировано: 13000
   - Ошибок: 0
   - Время выполнения: 320с
   - Средняя скорость: 40 товаров/сек
```

## Устранение неполадок

### Ошибка "OZON API не настроен"

Убедитесь, что в настройках приложения указаны:
- Client ID
- API Key
- API включен (enabled = true)

### Ошибка "Rate limit exceeded"

Система автоматически обрабатывает rate limits, но если ошибки продолжаются:
- Увеличьте задержки между запросами в `ozonService.ts` (параметр `delay`)
- Уменьшите размер батчей (параметр `limit`)

### Медленная синхронизация

- Проверьте скорость интернет-соединения
- Убедитесь, что сервер не перегружен
- Проверьте логи на наличие ошибок

### Товары не обновляются

- Проверьте, что синхронизация завершилась успешно
- Проверьте логи на наличие ошибок
- Убедитесь, что данные в OZON актуальны

## Технические детали

### Курсорная пагинация

Вместо offset-based пагинации используется курсорная (`last_id`), что позволяет:
- Быстрее обрабатывать большие объемы данных
- Избежать проблем с пропуском/дублированием товаров при обновлениях
- Снизить нагрузку на API

### Параллельная загрузка

Данные загружаются параллельно в несколько этапов:
1. Сначала получаем список всех товаров
2. Затем параллельно загружаем цены, остатки и изображения
3. Объединяем все данные и сохраняем в БД

### Bulk Operations

Для быстрого сохранения используется MongoDB `bulkWrite` с батчами по 1000 операций, что позволяет:
- Минимизировать количество запросов к БД
- Ускорить процесс сохранения
- Обработать ошибки на уровне батчей

