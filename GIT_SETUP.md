# 📦 Настройка Git репозитория

## Инициализация и первый коммит

Выполните следующие команды в корне проекта:

```bash
# Инициализация репозитория
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "first commit"

# Переименование ветки в main
git branch -M main

# Добавление удалённого репозитория
git remote add origin https://github.com/Seb0g1/Invoice-Manager.git

# Отправка в репозиторий
git push -u origin main
```

## Перед коммитом

Убедитесь, что:

1. ✅ Проект собран (`npm run build` в backend и frontend)
2. ✅ Файл `.env` добавлен в `.gitignore` (не коммитится)
3. ✅ Все зависимости установлены
4. ✅ Тестовые аккаунты удалены из Login.tsx

## Структура для коммита

```
Invoice-Manager/
├── .gitignore          ✅
├── README.md           ✅
├── DEPLOY.md           ✅
├── setup.sh            ✅
├── start.sh            ✅
├── backend/
│   ├── src/            ✅
│   ├── package.json    ✅
│   └── .env.example    ✅ (не .env!)
└── frontend/
    ├── src/            ✅
    └── package.json    ✅
```

## Важно!

- ❌ **НЕ коммитьте** файл `.env` с реальными паролями
- ❌ **НЕ коммитьте** `node_modules/`
- ❌ **НЕ коммитьте** загруженные файлы из `backend/uploads/` (только структуру)
- ✅ Используйте `.env.example` как шаблон

